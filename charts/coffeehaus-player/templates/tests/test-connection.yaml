apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coffeehaus-player.fullname" . }}-test-connection"
  labels:
{{ include "coffeehaus-player.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "coffeehaus-player.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
